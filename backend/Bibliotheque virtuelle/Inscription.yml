info:
  name: Inscription
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/api/auth/register"
  body:
    type: json
    data: |-
      {
        "email": "nouveau@test.fr",
        "firstName": "Jean",
        "lastName": "Test",
        "password": "password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
